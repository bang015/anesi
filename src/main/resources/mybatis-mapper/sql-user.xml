<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.anesi.mapper.user">
	<!-- 로그인 쿼리 -->
	<select id="selectUser" parameterType="hashmap" resultType="com.example.anesi.model.User">
		SELECT * 
		FROM TBL_USER
		WHERE U_ID = #{userId} AND PWD = #{pwd}
	</select>
	<!-- 로그인 아이디 확인 -->
	<select id="userCnt" parameterType="hashmap" resultType="int">
		select count(*) as cnt
		from tbl_user
		where u_id = #{userId}
	</select>
</mapper>